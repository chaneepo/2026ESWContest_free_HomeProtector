# 웹 서버 전용 장치 연결 / Server-only device endpoints
# Copy to frontend/.env.local only when connecting verified devices.
# Use a trusted LAN or SSH tunnel; do not expose devices publicly.
# No configured endpoint means no real-device connection.
RASPBOT_API_URL=
CAMERA_API_URL=
